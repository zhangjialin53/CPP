//文件 - 6 - 修理复读机
//时间限制	1000ms	空间限制	256MB
//
//有归属就有宁静
//
//可是平淡的生活
//
//就总有人无风起浪
//
//这一次大姐头还没来得及看你
//
//你就已经站了出来
//
//这就是你的使命
//
//你要找到他们 消灭他们
//
//就像消除错误
//
//或许他们的出现本就是清澈生命里的浑浊
//
//你发现 每三个数字里
//
//第一个属于善良的生命
//
//后面两个是祸乱的起因
//
//于是你的责任 义不容辞
//
//消除祸乱 还世界一个清宁
//
//Be a lover, be a trouble maker.
//
//TO - DO
//请你编写一个程序，将该文件去噪并输出原始内容，文件内容你可以点击下载 查看。
//
//文件以 3 字节为步长的加噪音字符串，例如：A man 增加噪音后变为 A & c )4m - Da0Tn
//
//注意：
//
//通过相对路径 noise.txt 的方式打开输入文件。
//输入数据说明
//本题文件内容为加噪音后的字符串，详情请下载查看。
//本题不需要考虑非法输入，输入文件保证为以 3 字节为步长的加噪音字符串，所有字符在可打印 ASCII 字符范围内(ASCII33 - ASCII126)。
//输出格式说明
//输出降噪后的源字符串
//限制
//不允许使用尚未讲授过的后续课程中的知识点
//过程中请给出相应的输入提示与输出提示
//程序编写应符合锯齿型的书写格式
//不允许使用 goto
//除明确要求外，不允许使用 C++ 的 std::string 类
//运行时间 1 秒
//运行使用内存 128MB
//输出限制 2048 个字符
//评分标准
//若出现非正常退出(Runtime Error)，按测试点视情况扣分
//【人工审核项】 若发现代码使用后续知识点，视情况扣分
//【人工审核项】 若代码书写不规范或整洁度极差(如变量取名不规范或没有按照锯齿型书写等)，视情况扣分
//【人工审核项】 不符合题目要求（如要求用递归完成而没有使用、没有错误性输入检测等），视情况扣分
//直接输出答案而非程序运行结果的，0 分
//抄袭舞弊者，0 分

#include <iostream>
#include <fstream>
#include <limits>

using namespace std;

// 主函数
int main() {
    ifstream file("noise.txt");
    if (!file.is_open()) {
        cerr << "无法打开文件noise.txt，请确认文件存在且路径正确。" << endl;
        return 1;
    }

    cout << "-*以下是文件内容*-" << endl;

    char ch;
    while (file.get(ch)) {
        // 输出当前字符
        cout << ch;

        // 跳过接下来的两个字符（假设它们是噪声）
        if (file.get(ch) && file.get(ch)) {
            continue;
        }
        else {
            // 如果提前到达文件末尾，跳出循环
            break;
        }
    }

    file.close(); // 关闭文件
    return 0;
}